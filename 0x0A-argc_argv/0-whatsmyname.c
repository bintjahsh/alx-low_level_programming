#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints the name of the program
 *
 * return: 0 if main is succesful
 */
int main (int argc, char *argv[])
{
	printf("%d\n", argv[0]);

	return (0);
}
